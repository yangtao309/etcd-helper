#!/bin/sh

init() {
    echo "This is etcd delete shell, welcome!"
    export CURRENT_DIR=$PWD
}

usage() {
    echo "usage: etcd delete <options>"
    echo "功能说明:"
    echo "  删除目录或者文件"
    echo "参数说明:"

}

cmd_help() {
    usage
    exit 0
}

cmd_default() {
    echo "function cmd_default."
}
